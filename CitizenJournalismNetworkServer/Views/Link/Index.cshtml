@model IEnumerable<CitizenJournalismNetworkServer.Domain.Models.Link>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th></th>
        <th>
            Href
        </th>
        <th>
            Type
        </th>
        <th>
            Language
        </th>
        <th>
            Title
        </th>
        <th>
            Length
        </th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
        <td>
            @item.Href
        </td>
        <td>
            @item.Type
        </td>
        <td>
            @item.Language
        </td>
        <td>
            @item.Title
        </td>
        <td>
            @item.Length
        </td>
    </tr>
}

</table>

