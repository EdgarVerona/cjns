@using CitizenJournalismNetworkServer.Models
@using CitizenJournalismNetworkServer.Helpers
@model Workspace

{
    "title": "@Model.Title",
    "collections": @(JsonHelper.RenderJsonCollection<Collection>("_appCollection", Model.Collections, this.Page))
}