@using CitizenJournalismNetworkServer.Domain.Models
@model Workspace

<workspace>
    <atom:title>@Model.Title</atom:title>
    @foreach (Collection collection in Model.Collections)
    {
        @Html.Partial("_appCollection", collection);
    }
</workspace>


